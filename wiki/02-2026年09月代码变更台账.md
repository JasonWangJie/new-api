# 2026 年 9 月 Git 变更台账

本页按 Asia/Shanghai 时区记录 2026-09-17 00:00:00 起当前仓库全部可达 Git 引用中的提交。当前覆盖 9 个提交，截止 `4ddaa904351d6b78c6370f817849d278de582b10`。

查阅任一原始变更可运行：

```bash
git show <commit>
```

## 2026-09-17

### `c211e365027e79e0a195125227af87d7e9015642` — 建立异步图像完整体系

- 时间：2026-09-17 08:35:18 +08:00
- 作者：wangjie
- 规模：100 个文件，新增 19,219 行，删除 152 行。
- 后端新增异步图像请求接入、协议转换、渠道路由、计费衔接、任务 worker、失败处理、清理、加密、对象存储和 Redis 命名空间隔离。
- 新增图像模型、能力、任务、输入、日志、图库、广场和审核相关数据模型与管理接口。
- 新增图像工作台、Token 图像平台分组、用户及管理员任务中心、图库、广场、内容审核、系统设置和异步图像 API 指引页面。
- 新增图像管理权限、公开及管理路由、环境变量、Docker 镜像运行依赖、七种前端语言和后端/前端回归测试。
- 新增图像开发交接及当时的功能规格材料；这些历史文件只作为提交事实记录，后续补充说明统一维护在当前 Wiki。

### `46d0a0b9531a7ca19174c0f0d4e1e3132c95bb90` — 首页结构重构

- 时间：2026-09-17 10:03:30 +08:00
- 作者：王杰
- 规模：15 个文件，新增 2,889 行，删除 45 行。
- 公开首页改为独立 `CyberLanding` 组件，并调整公共布局与首页入口结构。
- 新增模型网络主视觉、能力区、API 协议引导、终端演示、响应式布局和专用 `home-cyber.css` 样式。
- 新增首页测试及七种语言文案。
- 同次提交加入 Docker 部署与更新指南、异步图像 API 接口材料。

### `cdd1f63a0e6cb6e7c76d590e0b007b87ea4ac32d` — 首页视觉与 Fork 部署工具

- 时间：2026-09-17 10:30:25 +08:00
- 作者：王杰
- 规模：20 个文件，新增 1,292 行，删除 191 行。
- 优化 Cyber Landing 的标题、动画、网络视觉、交互状态、响应式表现和测试覆盖，并同步七种语言。
- 新增 `deployment/fork-docker/`：包含 Compose 环境封装、PostgreSQL、Redis、应用容器、健康检查和日志轮转配置。
- 新增首次安装、离线导出、运行管理、备份、更新和恢复脚本，以及当时的工具压缩包。
- 新增 `.rules` 和临时 `AGENTS copy.md`；后者在同日后续提交中删除。

### `f10ffe5d1ead482f47af1fedc2bfe8ef866e72f0` — 异步图像任务管理与详情

- 时间：2026-09-17 14:13:14 +08:00
- 作者：王杰
- 规模：29 个文件，新增 2,107 行，删除 361 行。
- 扩展异步图像任务列表、详情和结果访问接口，并补充多数据库任务查询测试。
- 新增完整的图像任务详情组件，展示状态、规格、输入、结果、耗时和成功率。
- 重构任务中心展示和任务呈现工具，更新使用日志图像详情、对话框及移动筛选布局。
- 新增任务中心前端测试和七种语言词条。
- 删除临时 `AGENTS copy.md`，同时调整 `.rules`。

### `6e9a9c3192063d0b6e963bc3ebfe2d1c583a25f2` — 图像任务视觉和交互优化

- 时间：2026-09-17 15:51:18 +08:00
- 作者：王杰
- 规模：15 个文件，新增 645 行，删除 126 行。
- 新增任务状态、平台和请求类型徽章及统一视觉映射。
- 将任务中心自动刷新默认状态调整为关闭。
- 任务详情支持复制参考图像 URL 和在新标签页打开。
- 新增图像视觉工具测试，扩展任务中心测试并更新七种语言。
- 新增当时使用的 `.cursorrules`。

### `f5c05a1de478cbcd6384a9b7bf3791bf3f5ee2ea` — 图像任务查询与预取优化

- 时间：2026-09-17 16:17:47 +08:00
- 作者：王杰
- 规模：6 个文件，新增 94 行，删除 82 行。
- 调整后端任务获取逻辑，减少数据库查询次数。
- 任务中心加入详情预取，缩短用户打开详情时的等待。
- 图像 URL 改为按展示需要获取，避免工作台和详情页产生不必要请求。
- 同步更新任务中心回归测试。

### `75e4f734e4df372213242fd6067e4b0102d20e60` — 图像任务签名 URL

- 时间：2026-09-17 16:33:06 +08:00
- 作者：王杰
- 规模：6 个文件，新增 118 行，删除 43 行。
- 任务查询结果支持生成有时效的签名图像对象 URL。
- 图像任务详情改为异步获取结果 URL，并优化加载、打开和展示逻辑。
- 工作台和使用日志图像详情同步适配新的 URL 获取方式。
- 扩展任务中心测试，覆盖新的结果访问行为。

## 2026-09-19

### `6f340ebe16abfeafec10e5d20dfa11dad1e7b2c4` — 异步视频与统一媒体任务

- 时间：2026-09-19 23:26:32 +08:00
- 作者：wangjie
- 规模：90 个文件，新增 6,900 行，删除 1,059 行。
- 新增异步视频请求准备、渠道筛选、任务提交、状态查询、重试、终止、持久化和响应协议。
- 新增统一 `AsyncMediaJob`、媒体对象模型、租约式 worker、本地媒体存储、签名下载、输出归档和清理。
- 扩展图像能力描述和多个渠道适配器，并更新 Alibaba、Google、Vertex AI 等任务插件。
- 新增用户及管理员媒体任务接口、媒体运行配置、视频工作台和视频结果组件。
- 图像工作台与任务中心统一承载图像和视频任务，并同步 API 指引、七种语言和测试。

## 2026-09-20

### `74c89aea2` — Gemini 异步错误诊断与 611–613 分类修复

- 记录时间：2026-09-20 20:48:26 +08:00
- 提交哈希：`74c89aea2b7652ea3bb77b05ddb64ec58c36b5e2`
- 变更范围：Gemini 异步图片非 200 响应解析、任务失败分类、尝试审计、管理员任务详情和回归测试。
- 在通用 Gemini 兼容错误归一化前读取有界的原始错误对象，保存经过 URL、凭证和长度处理的 `error.message`，避免 HTTP 400 详情被统一压缩成 `Invalid request`。
- 分类同时参考供应商 `error.code`、canonical status 和原始消息，使参考图超限、缺图及无法处理输入分别稳定落到 611、612、613；未知 400 继续使用 610。
- 既有 `attempts` 持久化快照新增 HTTP 状态、脱敏错误、provider code/status 和上游 request ID，不新增数据库列；结构化字段仅由管理员任务详情返回。
- 新增 Gemini 错误解析、脱敏、分类、持久化和管理员/普通用户隔离回归用例。
- 验证：`go test ./service ./relay ./controller ./model ./router ./relay/channel/gemini -count=1` 与 `go vet ./service ./relay ./controller ./router` 通过。

### 待提交（基于 `ead8bfccc214eb50e242ed809efe8167756ef76b`）— 异步生图排队与上游轮询修复

- 记录时间：2026-09-20 15:35:23 +08:00
- 变更范围：异步图像 outbox dispatcher、worker 租约、延时轮询、公共/站内状态口径和回归测试。
- 新增独立的秒级 outbox 投递循环，新任务在正常空闲时不再依赖默认 30 秒恢复扫描才进入 Redis 队列。
- 上游已返回任务 ID 时，每轮查询后释放数据库租约，并使 outbox 和任务共用 `next_attempt_at`，避免默认 120 秒租约和过早投递共同导致长时间假排队。
- 上游已接单的图像任务在公共查询、统一媒体阶段、任务中心列表、统计和状态筛选中统一归类为“处理中”；轮询抢占保留已冻结的渠道和首次派发时间。
- 新增 miniredis 回归用例，覆盖租约释放、延时命令不提前投递、到期投递、显示状态、崩溃恢复和轮询抢占不重置渠道。
- 数据库矩阵验证：SQLite 3.50.4、MySQL 8.4.11 和 PostgreSQL 16.14 下，`go test ./service -run TestAsyncImageKnownUpstreamPollReleasesLeaseAndStaysProcessing -count=1 -v` 与 `go test ./controller -run TestImageTaskPresentationDatabaseMatrix -count=1 -v` 全部通过；测试对相关模型连续执行两次 `AutoMigrate` 验证幂等性。
- 全仓后端回归 `go test ./...` 通过。
- 当前尚未创建 Git 提交；最终提交哈希需在提交时替换本条的“待提交”标识。

### 待提交（基于 `b3c6929de758bd626baf2f0f3ccffb3886767249`）— xAI 与 Seedance 异步视频编辑、延长

- 记录时间：2026-09-22 10:50:38 +08:00
- 提交哈希：待提交（基于 `b3c6929de758bd626baf2f0f3ccffb3886767249`）
- 变更范围：任务插件视频能力契约、标准视频任务路由、持久化异步媒体路由与 Worker、xAI/Seedance 插件、xAI 计费、视频工作台、平台策略与 API 示例、七种前端语言及功能说明。
- 新增 `POST /v1/videos/edits`、`POST /v1/videos/extensions`、`POST /v1/videos/edits_async` 和 `POST /v1/videos/extensions_async`。持久化任务继续复用统一查询、共享渠道池、加密载荷、媒体落盘、签名链接、结算、重试及 `execution_unknown` 防重提机制；未新增数据库表、列或迁移。
- `videoProfiles` 增加 `edit_video`、`extend_video`、可选模式维度和延长方向。宿主只为显式声明操作能力的模型绑定路由，校验必填主视频、来源、数量、锁定维度和策略缩限；旧插件和旧加密载荷继续兼容生成操作。
- xAI 仅为经典 `grok-imagine-video` 开启编辑和向后延长，接受 HTTPS MP4、`data:video/mp4` 或 `file_id`。编辑和延长使用专用上游路径，轮询仍复用视频任务路径；预扣采用安全上限，成功响应存在 `usage.cost_in_usd_ticks` 时按实际上游费用结算，经典模型兜底价格增加视频输入秒数。
- Seedance 2.0、Fast、Mini、2.5 支持编辑与前后延长。主视频始终作为第一个 `reference_video` 写入 Ark `content`，再合并多模态参考和原生内容；各模型的时长、分辨率、素材数量、自适应比例及 2.5 编辑 `duration=-1` 在插件解码阶段再次校验。
- 视频工作台按模式隐藏锁定维度、重置遗留值，动态展示主视频和延长方向，并自动选择编辑或延长异步端点；设置页与站内 API 指南加入双层端点、策略 JSON 和 xAI/Seedance curl 示例。新增文案已同步 en、zh、zh-TW、fr、ja、ru、vi。
- 本地验证覆盖插件契约与路由、xAI/Seedance 请求解码、策略缩限、计费表达式、异步路径幂等与加密操作恢复、标准视频回执、前端类型检查和能力表单。完整验证结果以本次任务最终交付说明为准。
- 未提供真实 xAI 或 Ark 有效账号，因此未执行真实上游提交、轮询、落盘和最终结算，不据此宣称上游账号或生产环境验收完成。
- 当前尚未创建 Git 提交；最终提交哈希需在提交时替换本条的“待提交”标识。

### 待提交（基于 `28878b52533b3f2d4534a26373acfb3356f8dd97`）— 渠道级上游异步任务动态解析

- 记录时间：2026-09-22 15:34:38 +08:00
- 提交哈希：待提交（基于 `28878b52533b3f2d4534a26373acfb3356f8dd97`）
- 变更范围：渠道设置契约、图片及视频异步提交与 Worker、动态轮询/状态/结果/用量解析、安全下载、渠道编辑表单、七种前端语言、Task Plugin API 覆盖说明、回归测试及功能说明。
- 渠道 `settings` 新增可选 `upstream_async.profiles`，最多 32 条，按媒体类型、映射后的上游模型和生成/编辑/延长操作唯一匹配。保存时严格校验重复/重叠选择器、受限 GJSON 路径、GET/POST 查询模板、允许的占位符、状态值类型与集合冲突、Usage 目标及下载头；配置仍存入原渠道 JSON，不新增表、列或迁移。
- 命中规则后，原插件继续负责请求构造、模型能力、计费及展示，宿主原子覆盖任务号提取、查询请求、状态映射和主结果提取。视频任务冻结无密钥 profile 快照及上游响应到私有任务数据；图片任务立即持久化上游任务号，后续 Worker 只轮询或重试落盘，不会重新生成。
- 图片可解析最多 `dto.MaxImageN` 个结果 URL，视频只接受一个主 URL。凭据下载头仅允许渠道同源，跨域结果必须匿名可访问；成功结果全部经过既有安全下载、校验、staging、媒体存储和签名流程，上游 URL、任务号及原始响应不会通过公开任务查询泄露。
- 状态值按 JSON 字符串、数字和布尔类型精确匹配，未知/缺失状态进入既有轮询失败预算，显式失败进入退款链。按次/按张计费可使用冻结估值，Token 计费缺少或得到越界实际用量时不公开结果，并最终以 `billing_usage_missing` 失败；`execution_unknown` 继续阻止重复上游提交。
- 渠道编辑页增加“上游异步任务”入口，提供结构化表单、高级 JSON、内置解析/渠道覆盖状态，以及图片多结果和视频单结果复制示例；新增文案覆盖 en、zh、zh-TW、fr、ja、ru、vi。
- 本地验证：相关 Go 回归、`relaykit` 独立构建/测试、前端目标 Vitest、TypeScript、改动文件 lint/format、i18n 同步及生产构建通过；根模块完整 Go 测试仅出现一次 Windows HTTP/2 GOAWAY 套接字波动，失败用例隔离复验通过。完整前端 Vitest 为 1749 通过、16 个既有侧栏/用量审计及并发超时失败；全量 lint/format 仍受既有基线问题阻断。数据库回读在 SQLite 通过，因未配置 `TEST_MYSQL_DSN`、`TEST_POSTGRES_DSN` 未执行 MySQL/PostgreSQL，不能宣称三数据库验收完成；未提供真实第三方上游账号，不据此宣称上游或生产环境验收完成。
- 当前尚未创建 Git 提交；最终提交哈希需在提交时替换本条的“待提交”标识。

### 待提交（基于 `28878b52533b3f2d4534a26373acfb3356f8dd97`）— ChannelOtherSettings 配置指南

- 记录时间：2026-09-22 20:20:56 +08:00
- 提交哈希：待提交（基于 `28878b52533b3f2d4534a26373acfb3356f8dd97`）
- 变更范围：`ChannelOtherSettings` 独立 Wiki、Wiki 目录与首页、根补充说明，以及异步图片和统一媒体页面的交叉入口。
- 新增全部顶层字段的类型、默认值、适用范围和行为说明，明确渠道接口 `setting` 与 `settings` 的差异，以及 REST 接口中 `settings` 仍为 JSON 字符串。
- 完整记录 `advanced_custom` 的路由、协议转换、模型匹配、鉴权和模型发现约束，以及 `upstream_async` 的规则选择、任务号路径、GET/POST 查询模板、精确状态值、结果、下载头和所有允许的 Usage 目标。
- 提供供应商密钥模式、字段透传、模型变化检测、异步图片多结果、异步视频 POST 查询、同源鉴权下载和常见组合示例，并补充排错清单与密钥安全建议。
- 本条仅增加和整理说明入口，没有改变运行时代码、接口契约、数据库结构或迁移。
- 当前尚未创建 Git 提交；最终提交哈希需在提交时替换本条的“待提交”标识。

### 待提交（基于 `ead8bfccc214eb50e242ed809efe8167756ef76b`）— 开票企业资料自动带入

- 记录时间：2026-09-20 15:31:04 +08:00
- 变更范围：用户开票配置接口、企业资料表单、最近资料查询及后端和前端回归测试。
- 用户再次申请开票时，自动带入最近一次成功提交的企业名称、企业税号和接收邮箱；资料仍可编辑。
- 修改后的资料仅在申请成功后成为下次默认值，提交失败不会覆盖此前保存的资料；管理员历史补开记录不会参与默认值选择。
- 复用现有 `invoice_requests` 数据，不新增数据库字段或迁移。
- 当前尚未创建 Git 提交；最终提交哈希需在提交时替换本条的“待提交”标识。

### 待提交 — 站内文档中心

- 记录时间：2026-09-20 15:20:00 +08:00
- 变更范围：顶部文档入口、`/docs` 路由、文档内容包、文档渲染组件、七种语言和回归测试。
- 顶部「文档」与首页 Hero 文档按钮改为站内 `/docs/`，不再打开外部文档链接。
- 新增类似 JuAPI 的文档中心：侧栏分组导航、正文块渲染、本页目录、上下篇导航；内容中的 `hejuapi.com` 已替换为 `https://api.aiimg.lol/`。
- 新增文档目录与页面渲染回归测试。
- 当前尚未创建 Git 提交；最终提交哈希需在提交时替换本条的“待提交”标识。

### 待提交 — 认证页赛博视觉与动效

- 记录时间：2026-09-21 09:50:00 +08:00
- 变更范围：登录 / 注册 / 找回密码布局、`auth-cyber.css`、认证装饰组件、七种语言和布局回归测试。
- 认证页改为与 Cyber Landing 一致的暗色科技风，并加强动效：透视网格、六边形网、雷达扫描、光晕轨道、星座连线、数据雨、流星、双扫描线、粒子、玻璃面板流光与呼吸光晕、标题渐变。
- 顶栏 / 面板 / 页脚入场动画与表单区错落入场；提供动画暂停/恢复；减少动态效果偏好下关闭入场动画。
- 新增认证布局回归测试，覆盖品牌入口、面板结构、雷达/雨滴/流星装饰与动效开关。
- 当前尚未创建 Git 提交；最终提交哈希需在提交时替换本条的“待提交”标识。

### 待提交（基于 `ead8bfccc214eb50e242ed809efe8167756ef76b`）— 首页中心站点名与字号

- 记录时间：2026-09-20 14:56:00 +08:00
- 变更范围：Cyber Landing 中心品牌文案、`home-cyber.css` 字号、协议区入口与首页回归测试。
- 模型网络中心 `cyber-core` 改为读取 `/api/status` 的 `system_name` 展示站点名称；空值回退为默认站点名。
- 放大中心品牌与“统一 API 网关”副标题字号，并限制过长站点名在核心区内省略显示。
- 移除协议区“阅读接入文档”入口及相关样式。
- 新增站点名填充与默认回退回归测试，并确认文档入口不再渲染。
- 当前尚未创建 Git 提交；最终提交哈希需在提交时替换本条的“待提交”标识。

### 待提交（基于 `75aec941a4880eafcfc3279f2b7768136a71a861`）— 图像工作台滚动修复

- 记录时间：2026-09-20 13:36:01 +08:00
- 变更范围：图像工作台前端布局、布局回归测试和功能说明。
- 工作台外层改为可收缩的纵向弹性容器，媒体切换栏保持固定，内部页面占用剩余高度。
- 选择 `gpt-image-2` 等会展开额外参数的模型后，内容由现有页面滚动区承载，底部参数和生成按钮不再被裁掉。
- 新增布局回归测试，固定高度传递、收缩和溢出策略。
- 当前尚未创建 Git 提交；最终提交哈希需在提交时替换本条的“待提交”标识。

### 待提交（基于 `75aec941a4880eafcfc3279f2b7768136a71a861`）— 开票操作与付款方式展示修复

- 记录时间：2026-09-20 13:43:52 +08:00
- 变更范围：用户开票申请、管理员历史补开、开票订单接口、订单明细和回归测试。
- 将两个页面级确认弹窗移出只接收命名插槽的布局组件，使用户“申请开票”和管理员“标记开票”操作能够正常打开弹窗并发起请求。
- 可开票订单和开票申请明细返回充值订单的实际付款方式，并在订单选择表和记录详情中显示；未新增数据库字段或迁移。
- 新增页面级提交链路、管理员申请完成链路、付款方式接口与详情展示回归测试。
- 当前尚未创建 Git 提交；最终提交哈希需在提交时替换本条的“待提交”标识。

### `4ddaa904351d6b78c6370f817849d278de582b10` — 企业开票申请与历史补开

- 时间：2026-09-20 10:23:24 +08:00
- 作者：wangjie
- 规模：44 个文件，新增 3,952 行，删除 11 行。
- 新增用户开票配置读取、可开票订单、申请创建、记录列表和详情接口。
- 新增管理员全站记录、完成、拒绝、指定用户订单和历史补开接口，以及 `invoice.read/manage` 权限。
- 新增申请、订单快照和订单唯一占用三张表，事务内复核金额、归属、来源和占用状态。
- 新增用户及管理员开票页面、跨页及移动端批选、企业资料表单、拒绝原因、操作人和处理时间展示。
- 新增计费设置中的开票开关和最低金额，并补齐审计日志、七种语言和后端/前端测试。
- 详细规则见 [企业开票申请与历史补开](06-企业开票.md)。

## 2026-09-21

### 待提交（基于 `4f6a36ee44a230f8f87efd192a10c8c6be203990`）— 媒体配置保存反馈修复

- 记录时间：2026-09-21 09:41:04 +08:00
- 变更范围：异步视频与本地存储设置、保存交互回归测试及功能说明。
- `/system-settings/operations/images` 的媒体运行配置在服务端保存成功后立即显示“已保存”反馈，并继续刷新最新配置，避免快速请求完成后界面看起来没有响应。
- 新增前端交互回归用例，覆盖修改本地存储目录、提交完整配置和成功反馈。
- 当前尚未创建 Git 提交；最终提交哈希需在提交时替换本条的“待提交”标识。

### 待提交（基于 `4f6a36ee44a230f8f87efd192a10c8c6be203990`）— 图片平台策略模型能力示例

- 记录时间：2026-09-21 09:51:32 +08:00
- 变更范围：图片平台策略设置、模型能力示例弹窗、复制交互、七种语言、前端回归测试及功能说明。
- `/system-settings/operations/images` 的“平台策略”在模型能力输入区提供配置示例按钮，并随平台选择切换 OpenAI、Gemini、Vertex AI 和其他已注册供应商的 JSON 模板。
- Gemini 模板展示原生图片模型的单图请求约束；Vertex AI 模板还展示 Imagen 的分辨率及参考图约束；未知动态平台使用明确的占位模型，避免误用其他供应商的模型名。
- 示例弹窗说明各能力数组的用途，支持一键复制完整 JSON，不会自动覆盖管理员尚未保存的表单内容。
- 新增前端交互回归用例，覆盖平台切换后的示例内容、复制结果和动态平台回退模板。
- 当前尚未创建 Git 提交；最终提交哈希需在提交时替换本条的“待提交”标识。

### 待提交（基于 `3ac4b1f2eb789038c3dad3576ea043f4666efd32`）— 媒体渠道池平台联动选择

- 记录时间：2026-09-21 11:17:34 +08:00
- 提交哈希：待提交（基于 `3ac4b1f2eb789038c3dad3576ea043f4666efd32`）
- 变更范围：`/system-settings/operations/images` 渠道池表单、候选查询与保存接口、图片及视频平台能力过滤、后端和前端回归测试及功能说明。
- 模型改为从当前分组和平台可执行的模型中选择；渠道改为显示 ID 与名称的多选项，并在选定模型后仅保留能力匹配、状态启用且已配置对应分组能力的渠道。
- 切换分组、平台、绑定方式或模型时清理不再适用的选择；编辑已有绑定会回填原分组、平台、模型、分辨率、渠道和优先级，失效的历史值显示为不可用而不会被静默替换。
- 保存接口再次验证模型是否属于当前分组和平台，并验证每个渠道是否属于对应候选集合，拒绝手工构造的模型名和无效渠道 ID；空列表继续保留显式关闭有效绑定的原语义。
- 验证：SQLite 3.50.4 下候选查询、有效保存、跨平台渠道拒绝和手工模型拒绝用例通过，`go test ./...`、`go vet ./service ./controller ./router`、图像前端 5 个测试文件共 15 项、TypeScript 类型检查、涉及文件 lint 和生产构建通过。当前环境没有可用 MySQL 服务，PostgreSQL 16 本机实例要求未提供的密码，因此尚未完成 MySQL/PostgreSQL 实例矩阵，不能据此宣称完整三数据库兼容性验收完成。
- 当前尚未创建 Git 提交；最终提交哈希需在提交时替换本条的“待提交”标识。

### 待提交（基于 `3ac4b1f2eb789038c3dad3576ea043f4666efd32`）— 公开首页科技动效加强

- 记录时间：2026-09-21 12:31:00 +08:00
- 提交哈希：待提交（基于 `3ac4b1f2eb789038c3dad3576ea043f4666efd32`）
- 变更范围：公开首页 `CyberLanding`、`home-cyber.css`、首页回归测试及功能说明。
- 默认首页在原有轨道、数据流和卡片扫描之外，增加网格漂移、扫描光带、粒子、雷达扇面、核心脉冲、节点浮动、集成区扫光、协议标签呼吸、步骤光迹和底部网格流动。
- 新增粒子层保持 `aria-hidden`，不进入读屏内容；页内“暂停动画”和系统“减少动态效果”会停止上述装饰动画。
- 模型网络中的 OpenAI 节点改为品牌标识，不再显示字母 O。
- 当前尚未创建 Git 提交；最终提交哈希需在提交时替换本条的“待提交”标识。

### 待提交（基于 `579d578ed52b83c6d053b8001fc737b3421cdba1`）— 生图渠道参考图容量优先调度

- 记录时间：2026-09-21 13:28:33 +08:00
- 提交哈希：待提交（基于 `579d578ed52b83c6d053b8001fc737b3421cdba1`）
- 变更范围：渠道结构化设置与编辑表单、异步图像策略解析和渠道调度、图像运行默认值、平台策略示例、七种语言、回归测试及功能说明。
- 渠道路由策略新增 `image_max_reference_images`；未设置的历史渠道按 8 张兼容，保存时校验为 0–128 的整数，不新增数据库字段或迁移。
- 异步任务参考图超过 8 张时，策略解析和最终渠道选择都优先采用声明容量覆盖本次请求的渠道，再执行原有优先级与权重规则；没有任何候选覆盖时回退原有候选集，不把“优先”变成硬拒绝。
- 图像运行配置的全局默认 `max_reference_images` 和平台策略模型能力示例由 8 调整为 14；渠道字段放在编辑页“路由策略”中，并沿用渠道敏感设置写权限。
- 验证：新增 SQLite 路由回归覆盖 9 张优先扩展渠道、8 张保持原优先级和无覆盖渠道时回退；`go test -p 1 ./...`、`go vet ./service ./model`、`cd relaykit && GOWORK=off go test ./...`、前端 2 个测试文件共 68 项、TypeScript 类型检查、涉及文件 lint/format、i18n 同步和生产构建通过。Windows 下并发执行 `go test ./...` 时既有 HTTP/2 graceful GOAWAY 用例出现本机套接字中止；该包单独执行及串行全量执行均通过。
- 当前尚未创建 Git 提交；最终提交哈希需在提交时替换本条的“待提交”标识。

### 待提交（基于 `579d578ed52b83c6d053b8001fc737b3421cdba1`）— 任务详情一键复制全部参考图链接

- 记录时间：2026-09-21 14:45:00 +08:00
- 提交哈希：待提交（基于 `579d578ed52b83c6d053b8001fc737b3421cdba1`）
- 变更范围：异步图像任务详情参考图区域、七种语言、任务中心回归测试及功能说明。
- 管理员任务详情在参考图标题旁增加「全部复制」；点击后把全部 `http(s)` 参考图 URL 按行写入剪贴板，内嵌图不进入复制内容。
- 用户任务详情仍不展示参考图与批量复制入口，行为与既有权限边界一致。
- 当前尚未创建 Git 提交；最终提交哈希需在提交时替换本条的“待提交”标识。

### 待提交（基于 `37e32db18762b943c0a1fc329b09280db0f17bd0`）— Gemini 参考图传输与校验修复

- 记录时间：2026-09-21 16:20:13 +08:00
- 提交哈希：待提交（基于 `37e32db18762b943c0a1fc329b09280db0f17bd0`）
- 变更范围：异步图像运行配置、Gemini/OpenAI 参考图构造、图片容器校验、worker 错误分类、工作台能力与运行设置、后端和前端回归测试及功能说明。
- 修复 Gemini `passthrough_fallback_local` 首次执行仍先由服务器下载的问题：普通外部 URL 首次直接交给上游，SC/内嵌/本地模式才本地读取，上游拉取失败后的参考图重试切换为本地下载。
- 删除参考图合计字节数和合计像素数限制，继续保留单图字节数、单图像素数与最大参考图数量限制；将全局 `max_reference_images` 提升为普通表单项，让工作台展示模型与全局配置的有效较小上限，并移除按 Flash 模型名固定 3 张的隐藏解析限制，具体模型上限统一由平台策略声明。
- 对 PNG、JPEG、WebP 仅规范化终止标记后不超过 4 KiB 的尾随数据，裁剪后再次完整解码并以规范字节参与哈希和传输；损坏容器和更大尾随区仍拒绝。
- 本地参考图错误标明从 1 开始的图片序号且不写入签名 URL；格式、容器、MIME、单图字节和像素超限归为不可重试输入错误，网络类错误仍按原参考图重试预算处理。
- 高级运行配置继续采用严格 JSON；JSON 注释不会生效并会导致保存解析失败。
- 验证：`go test -p 1 ./...`、`go vet ./service ./relay ./controller`、运行设置 Vitest 回归、TypeScript `tsgo -b`、涉及文件 oxlint/oxfmt 和 Rsbuild 生产构建通过。此次没有数据库结构、迁移或方言 SQL 变更，也未部署到生产环境执行真实 Gemini 账号验收。
- 当前尚未创建 Git 提交；最终提交哈希需在提交时替换本条的“待提交”标识。

### 待提交（基于 `b3c6929de758bd626baf2f0f3ccffb3886767249`）— Gemini 模糊 400 本地回退与运行字段说明

- 记录时间：2026-09-21 20:36:16 +08:00
- 提交哈希：待提交（基于 `b3c6929de758bd626baf2f0f3ccffb3886767249`）
- 变更范围：Gemini 异步图片失败判定、参考图传输重试、任务尝试审计、图像高级运行设置、七种前端语言、回归测试及功能说明。
- 当 `gemini_reference_transport_mode` 为 `passthrough_fallback_local`、首次请求实际发送远程 `fileData`，且兼容渠道返回 `400 / INVALID_ARGUMENT` 并落入安全策略、未知错误或输入无法处理分类时，允许且只允许一次本地下载回退；明确的参考图数量超限、缺图和参数错误继续直接失败。
- 回退不把原始错误伪装成通用参考图拉取失败：首次尝试仍保存原来的公共错误码、HTTP 状态、脱敏消息和 provider code/status，同时标记为参考图传输失败，下一次尝试切换到 `local`。
- 本地重试成功进入结果持久化时清空任务顶层的旧错误码、错误说明和公共错误码，避免成功任务继续向 API 返回上一次失败；管理员尝试审计仍保留首次失败详情。
- 高级运行配置改用项目现有 JSON 编辑器并明确提示只接受严格 JSON；字段注释放在编辑器外的可折叠说明表中，覆盖全部运行字段、用途和当前值，不会污染实际保存的 JSON。
- 验证：`go test -p 1 ./...`、`go vet ./service ./relay ./controller`、运行设置 Vitest、TypeScript `tsgo -b`、涉及文件 oxlint/oxfmt、i18n 同步和 Rsbuild 生产构建通过。重试调度与成功清错回归在 SQLite、MySQL 8.4.11、PostgreSQL 15.19 三种真实数据库通过。
- 隔离实测：在服务器克隆库、独立 Redis 前缀和独立容器中，Gemini 10 张参考图首次远程透传可直接成功；通过临时代理稳定注入首个 `400 / INVALID_ARGUMENT` 后，任务以 `retry_count=1`、`reference_retry_count=1` 切换到 `local`，第二次请求成功产出 1024×1024 JPEG。生产容器、生产 `newapi` 数据库及生产任务均未改动，全部临时容器、数据库、Redis 键、文件和临时 MySQL 镜像已清理。
- 本次不新增数据库字段、迁移或方言 SQL。当前尚未创建 Git 提交；最终提交哈希需在提交时替换本条的“待提交”标识。

### 待提交（基于 `b3c6929de758bd626baf2f0f3ccffb3886767249`）— 异步视频配置、Seedance 与 xAI 支持

- 记录时间：2026-09-22 00:13:28 +08:00
- 提交哈希：待提交（基于 `b3c6929de758bd626baf2f0f3ccffb3886767249`）
- 变更范围：任务插件视频能力契约、媒体平台策略、视频能力接口与工作台、共享渠道池、Seedance 插件、xAI 插件、计费表达式、七种前端语言、回归测试及功能说明。
- 任务插件 API v1 增加可选 `videoProfiles`，宿主校验模型、模式、时长、默认值、输入来源和数量并深拷贝注册表快照；旧插件继续使用通用兜底能力。平台模型 JSON 改为图片/视频联合目录，`video_overrides` 只能缩小上游声明，缺少 `media_type` 的旧配置保持兼容。
- `/api/option/images` 增加不含秘密值的视频就绪检查和插件模板；能力接口返回每个视频模型的有效 profile、不可用原因及计费状态。视频与图片继续共用渠道池，480p/720p、1080p、4K 分别参与 1K、2K、4K 路由。
- 新增内置 xAI Grok 视频任务插件，支持 1.5、预览/日期别名及经典模型的文生、图生、参考和首尾帧请求、状态轮询、临时结果归档、用量事实和官方美元计费表达式；管理员显式计费配置保持优先。
- 完善 Seedance 顶层首帧、尾帧和多模态参考素材到 Ark `content` 的转换，保留原生 `metadata.content`，按模型声明并再次强制校验模式、时长、分辨率和数量；工作台提示使用公网 URL 或 `asset://`，不展示误导性的文件上传。Seedance 不新增内置美元价格或计费硬阻断。
- “图片设置”显示名调整为“媒体设置”，增加独立视频配置页签、就绪状态、模型导入、策略高级 JSON、计费入口和可复制的 Seedance/xAI 策略及 curl 示例；视频工作台改为能力驱动表单并保护结构化字段。
- 不新增数据库表、列或迁移。当前环境未提供真实 xAI/Ark 有效账号，因此未执行真实上游提交、轮询、落盘和结算冒烟，不据此宣称外部或生产验收完成。
- 验证：`go test -p 1 ./... -count=1`、插件/服务/控制器等针对性 Go 回归、4 个前端测试文件共 11 项、TypeScript `tsgo -b`、涉及文件 oxlint/oxfmt、i18n 同步、Schema JSON 解析、`git diff --check` 和 Rsbuild 生产构建通过。全仓前端 oxlint 仍被本次变更外既有的 182 个错误阻断；受 Windows 文件占用影响，全仓受保护头格式脚本未完成快照恢复检查，涉及文件格式检查已单独通过。
- 当前尚未创建 Git 提交；最终提交哈希需在提交时替换本条的“待提交”标识。

### 待提交（基于 `fe7664d320e16c3e701878acf5358a40a8355c5b`）— 上游异步任务自定义提交请求

- 记录时间：2026-09-23 11:16:12 +08:00
- 提交哈希：待提交（基于 `fe7664d320e16c3e701878acf5358a40a8355c5b`）
- 变更范围：图片与视频异步提交链路、渠道 `upstream_async` 规则校验与模板、结构化渠道表单、七种前端语言、回归测试及功能说明。
- 每条上游异步规则可选择原适配器/任务插件默认提交，或设置同源 POST 路径、查询参数、请求头和 JSON 请求体模板；`{request.field}` 保留原 JSON 类型，缺失对象字段可省略。提交任务号、轮询、落盘和既有计费/能力校验仍走原任务流程。
- 增加 Mai Token `seedream-5-pro` 生图示例：图片任务提交到其 `/v1/videos`，查询 `/v1/videos/{task_id}`，从 `video_url` 取得图片结果；视频自定义提交仍需模型任务插件能力。
- 验证：Go 的服务模板与安全校验测试通过；渠道设置 SQLite 往返通过，MySQL/PostgreSQL 因未提供测试 DSN 跳过；`relaykit` 独立构建、Go 提交链路构建、前端类型检查、配置表单单测与渠道编辑交互测试、涉及文件 oxlint/oxfmt 和 `git diff --check` 通过。未使用真实 Mai Token 账号执行外部验收。
- 复用现有渠道 `settings` JSON，不新增表、列或迁移。当前尚未创建 Git 提交；最终提交哈希需在提交时替换本条的“待提交”标识。

### `3e4b56def11fe107f58681f140e3273369495f10` — 异步图片上游任务号持久化修复

- 记录时间：2026-09-23 14:37:48 +08:00
- 提交哈希：`3e4b56def11fe107f58681f140e3273369495f10`
- 变更范围：异步图片上游任务号持久化、图生图操作识别、结果下载瞬时失败重试、回归测试、Mai Token 生图配置与任务查询说明。
- worker 提交上游任务后会先更新本地任务版本；响应处理现在读取当前执行租约下的最新任务，再按版本校验保存上游任务号。重复的相同任务号保持幂等，不同任务号拒绝覆盖。
- 带参考图的公开生成请求保持 `generate` 规则选择，不再因内部适配器采用编辑路径而绕过自定义上游提交。
- 异步结果下载遇到网络或响应体读取中断时，按既有瞬时失败预算重新轮询已保存的上游任务号；不重新提交生成，也不把无效图片当成网络故障重试。
- 正式环境配置修正：`seedream-5-pro` 图片策略允许 8 张参考图；Mai Token 图片渠道使用顶层 `id` 读取提交任务号、`GET /v1/videos/{task_id}` 查询状态，并从实测的 `metadata.url` 读取图片结果。公开 `query_url` 使用 GET。
- 正式环境中一次图生图排队超过原有 720 秒执行期限，现将执行期限设置为 1800 秒以覆盖该渠道的实际排队时间；账户单次请求和下载超时仍由各自的原配置约束。
- 不新增数据库表、列或迁移；服务端渠道及模型策略变更属于现有配置数据。
- 验证：`go test ./relay ./service ./router -count=1` 通过；任务号持久化回归在 SQLite 3.50.4、MySQL 8.4.11、PostgreSQL 15.19 的独立测试库运行 `go test ./relay -run '^TestRecordAsyncImageUpstreamJobAfterDispatch$' -count=1` 均通过。正式环境的文生图及带一张参考图生图均返回 202，任务最终为 `succeeded`、`billing_status=succeeded`、结果数量 1；公开 `query_url` 使用 GET 均返回 200，两个本地结果链接均返回 PNG/HTTP 200。

### `5ca2f0dc939dc0eeffe7a8b5bb41d2ea8bdf38ee` — 上游异步媒体结果下载恢复

- 记录时间：2026-09-23 15:37:11 +08:00
- 提交哈希：`5ca2f0dc939dc0eeffe7a8b5bb41d2ea8bdf38ee`
- 变更范围：异步图片结果下载、视频媒体代理和本地保存恢复、聚焦回归测试，以及异步媒体与渠道配置说明。
- 图片结果下载的网络中断、响应体中断、HTTP 408、429、5xx 进入既有瞬时失败预算；重新轮询已保存的上游任务号，不重新提交生成。其余 HTTP 4xx、无效媒体和超限仍按不可重试结果处理。
- 同源鉴权下载端点允许跳转到公网 CDN，但跨域请求移除渠道凭据并重新校验目标地址；初始跨域结果仍须匿名可访问。图片和视频公开接口都只返回完成校验与落盘后的本站签名链接，不直接暴露供应商签名 URL。
- 视频在存储失败或中断后的恢复阶段按冻结的动态轮询规则刷新已完成任务的结果 URL；此前恢复路径错误使用任务插件默认查询接口，且会反复使用可能过期的保存链接。刷新只查询原任务，不重复生成或计费。
- 验证：`go test ./controller ./relay ./service ./router -count=1` 通过，覆盖同源鉴权跳转、跨域凭据清除、私有地址拒绝、图片瞬时 HTTP 状态重试、视频动态轮询刷新及本地 MP4 保存。正式环境只读检查时视频异步任务数为 0，因此没有真实视频供应商验收；新代码尚未部署。未改动数据库结构或迁移。
