# 异步图像、图库与图像广场

本页汇总 2026-09-17 的异步图像相关提交：`c211e365`、`f10ffe5d`、`6e9a9c31`、`f5c05a1d` 和 `75e4f734`。

## 功能组成

### 异步图像 API

公开接口位于 `/v1`，支持异步生成、编辑、任务查询和输入上传：

- `POST /v1/images/generations_async`
- `POST /v1/images/edits_async`
- `POST /v1/images/generations_oa`
- `POST /v1/images/edits_oa`
- `POST /v1/chat/completions_gm`
- `POST /v1/images/generations_sc`
- `POST /v1/uploads/images_sc`
- `GET /v1/images/tasks_async/:task_id`
- `GET /v1/tasks_sc/:task_id`

请求经过 Token 验证、Token 图像平台权限、协议识别、能力校验、渠道路由、计费预处理和异步任务持久化。任务 worker 使用租约避免多个实例重复处理，并记录重试、失败和结算状态。

#### 队列与上游轮询

- 新任务在数据库中与 event/outbox 同事务落库，outbox dispatcher 以 1 秒节拍投递可执行命令；正常空闲时不再等待默认 30 秒恢复扫描。
- `queued` 仅表示尚未被 worker 抢占或容量闸门已满。worker 抢占且选定渠道后对外显示“处理中”。
- Alibaba、Replicate 等先返回上游任务 ID 的渠道，在上游已接单后持续显示“处理中”。每次查询完成后释放数据库执行租约，按任务的 `next_attempt_at` 再投递，不会被默认 120 秒租约挡住。
- Redis 队列仍然是可重建的投递层；周期恢复扫描保留，用于 Redis 数据丢失、进程中断或临时连接失败后的收敛。

#### 渠道级上游异步解析

- 渠道 `settings` 可选配置 `upstream_async.profiles`，按图片类型、映射后的上游模型和 `generate`/`edit` 操作选择唯一规则。模型数组为空代表该渠道全部上游模型；重叠选择器、图片 `extend`、非法受限 GJSON 路径或超过 32 条规则会在保存时拒绝。
- 首次提交仍由原图片适配器构造请求并执行计费准备；命中规则后，宿主从配置的 `submit.task_id_path` 提取字符串或数字任务号并立即持久化。后续 Worker 只查询同一上游任务，不会重新发起生成；`execution_unknown` 仍作为禁止自动重提的屏障。
- 查询支持同一渠道 Base URL 下的 GET/POST、查询参数、JSON 请求体和请求头模板，占位符限于 `{task_id}`、`{model}`、`{upstream_model}`、`{api_key}`。状态值按 JSON 字符串、数字、布尔类型精确匹配；未知或缺失状态进入既有轮询失败计数，不会默认为处理中。
- 成功结果可从一个字符串或字符串数组读取，最多 `dto.MaxImageN` 张。所有 URL 经现有安全下载、校验、staging 和存储链路落盘，公开任务只返回本地结果 URL；配置了凭据下载头时结果必须与渠道同源，跨域地址只能匿名下载。
- `usage_paths` 只能映射到现有 Usage 数值字段及 token detail 字段。按次或按张计费可沿用冻结估值；Token 计费必须在最终响应得到有效实际用量，否则保留上游任务、不泄露结果，并在既有重试预算耗尽后以 `billing_usage_missing` 失败。
- 命中规则的渠道只参与 `/v1/images/generations_async`、`/v1/images/edits_async` 和统一媒体异步入口；同步图片接口会排除这些异步专用匹配，避免把任务号回执解释为图片结果。未匹配规则的渠道行为完全不变。

#### Gemini 上游失败诊断

- Gemini 异步请求遇到非 200 响应时，在通用兼容错误归一化前读取有界的原始错误对象；任务公开失败说明保留经过 URL、凭证和长度处理后的 `error.message`，不再把详细的 HTTP 400 一律替换为 `Invalid request` 或固定概述。
- 611、612、613 分类同时使用脱敏消息、供应商 `error.code` 和 Gemini canonical status，分别识别参考图数量超限、缺少参考图以及提示词或输入图无法处理；未知 400 仍保持 610 兜底。
- 每次失败尝试在既有任务尝试快照中保存 HTTP 状态、脱敏错误、供应商 code/status 和上游 request ID；结构化诊断只进入管理员任务详情，普通用户不会获得渠道、账号或上游 request ID。

#### 参考图传输、校验与重试

- Gemini 的 `passthrough_fallback_local` 在首次执行时把普通外部 HTTPS URL 作为 `fileData.fileUri` 交给上游拉取，不会先在本服务器下载。SC 输入、`data:` 内嵌图和显式 `local` 模式仍由服务器读取。上游明确返回参考图拉取失败时会进入本地下载重试；对于部分 Gemini 兼容渠道返回的模糊 `400 / INVALID_ARGUMENT`，仅当首次请求确实含远程 `fileData`、错误分类为安全策略/未知错误/输入无法处理且总重试预算仍可用时，额外允许一次本地传输回退。数量超限、缺图和参数错误不会触发该回退，原始脱敏 provider code、status 和错误说明仍保存在尝试审计中；若本地重试成功，任务顶层的旧错误码和错误说明会被清空。
- 参考图合计字节数和合计像素数不再单独设限；每张图仍受 `download_max_bytes`、`download_max_pixels` 约束，请求仍受 `max_reference_images` 约束。这意味着任意一张超过单图上限的图片仍会被拒绝。
- PNG、JPEG、WebP 必须具有可完整解析的容器并能再次完整解码。终止标记后的尾随数据只有不超过 4 KiB 时才会被裁剪，后续哈希、缓存和上游内嵌传输均使用裁剪后的规范数据；更大的尾随区或损坏容器继续拒绝，不会直接放开 trailing data。
- 本地下载或校验错误使用从 1 开始的参考图序号，例如 `Reference image 3`，错误文本不包含原始签名 URL。图片格式、容器、MIME、单图字节数或单图像素数错误归为不可重试输入错误，避免同一坏图重复下载；网络、临时 HTTP 和读取失败仍可按参考图重试策略收敛。

### 图像工作台

入口：`/image-workbench`

- 根据当前用户和 Token 加载可用平台、模型、尺寸、质量、数量和参考图能力。
- 支持多种图像请求格式和本地参考图处理。
- 提交后显示异步状态和结果，并可进入任务中心继续跟踪。
- 工作台将图像/视频切换栏与内容区组成受视口约束的纵向布局；选择 `gpt-image-2` 等带额外能力参数的模型后，表单在剩余高度内滚动，所有动态参数和生成按钮都可达。
- Token 编辑界面可以配置图像平台分组，限制 Token 可使用的平台。

### 任务中心

用户入口：`/async-image-tasks`

管理员入口：`/admin/async-image-tasks`

- 支持分页、筛选、任务详情、输入参数、结果图像、耗时和成功率展示。
- 管理员可恢复或终止任务，并支持批量终止。
- 状态、平台和请求类型使用统一徽章与视觉映射。
- 自动刷新默认关闭，用户可按需开启。
- 任务详情采用预取；结果对象 URL 按需异步获取。
- 管理员任务详情展示参考图列表，可单独复制或打开 URL，也可一键复制全部可访问链接（每行一个）；内嵌参考图不进入批量复制。

### 图像对象和签名 URL

- 图像结果保存为对象记录，业务数据保存对象标识和元数据，不在列表查询中直接加载大文件。
- 结果访问生成带过期时间的签名 URL，避免把永久存储地址直接暴露给客户端。
- 用户只能访问自己的任务结果；管理员查看需要对应权限。
- 后台清理任务负责临时对象、失败残留和过期数据。

### 个人图库

入口：`/image-library`

- 支持导入本地内容、远程 URL 和任务结果。
- 支持查看、修改、删除、发布和撤回作品。
- 发布可以直接提交，也可以创建延迟提交请求并同步处理状态。

### 图像广场和内容审核

用户入口：`/image-plaza`

管理员入口：`/admin/image-moderation`

- 广场展示已通过审核的公开作品。
- 用户可以提交作品、撤回发布和举报内容。
- 管理员可以审核发布申请、批量处理作品、查看举报并标记处理结果。
- 管理员可以查看图库统计、迁移状态和清理任务。

## 配置与权限

图像配置接口位于 `/api/option/images`，包含运行参数、渠道池、策略和存储配置。

平台策略页会根据当前选择的平台展示模型能力 JSON 示例入口。弹窗中的模板覆盖 OpenAI、Gemini、Vertex AI 及其他已注册图片供应商；动态注册且没有内置模板的平台使用带平台名的占位模型。Gemini 原生模型示例固定 `max_output_images` 为 `1`，Imagen 示例固定 `max_reference_images` 为 `0`。管理员应把模型 ID、可选能力和数量限制调整为渠道实际上游支持的值，再一键复制到“模型能力”输入框；示例只用于参考，不会自动覆盖当前表单。

渠道编辑页的“路由策略”可设置“最大参考图数量”。未设置的历史渠道按 8 张兼容；异步任务包含超过 8 张参考图时，自动平台策略解析会先比较渠道容量，最终渠道选择则在硬性能力、账号和熔断过滤之后优先保留容量覆盖本次请求的候选，再按原有优先级和权重选择。若当前候选没有任何渠道覆盖该数量，则保留原有候选继续调度而不是直接拒绝。图像运行配置的全局默认 `max_reference_images` 与平台策略中的参考图模型能力示例均调整为 14；模型自身能力仍会在接入校验阶段独立限制请求。

渠道编辑页同时提供“上游异步任务”入口。结构化视图覆盖媒体类型、模型、操作、任务号路径、查询请求、状态集合、进度、失败原因、结果和用量路径，高级 JSON 视图可编辑完整配置；页面会显示“内置解析”或“渠道覆盖”，并提供图片多结果与视频单结果示例。保存仍写入原有渠道 `settings` JSON，不新增数据库字段或迁移。

`ChannelOtherSettings` 顶层字段、`upstream_async` 各子字段、状态值规则、Usage 路径和完整可复制示例见 [渠道扩展设置](08-渠道扩展设置.md)。

全局 `max_reference_images` 已显示在图像运行设置的普通数字表单中，范围为 1–128，不再要求管理员打开高级 JSON 才能修改。工作台能力接口返回模型策略上限与全局上限的较小值，避免页面允许选择的数量在提交时又被全局配置拒绝；请求解析也不再根据模型名把 Flash 图片模型隐藏地硬编码为 3 张，具体模型上限以平台策略为准。高级运行设置使用带格式化与合法性状态的严格 JSON 编辑器：标准 JSON 不支持 `//`、`/* ... */` 或字段后注释，加入这些注释会导致保存解析失败。编辑器下方提供可折叠的“运行时字段说明”，逐项显示全部运行字段、用途和当前值；说明位于 JSON 外部，因此不会破坏配置语法，并已同步 en、zh、zh-TW、fr、ja、ru、vi 七种语言。

渠道池不再要求管理员手工填写模型名或逗号分隔的渠道 ID。设置页通过 `/api/option/images/pool/options`，按当前分组和平台列出平台策略允许、渠道能力支持且状态已启用的模型与渠道；选择模型后渠道列表进一步收窄，并以“渠道 ID + 渠道名称”展示。点击已有绑定的“编辑”会自动回填分组、平台、绑定方式、模型、分辨率、渠道和优先级；已经失效的旧值仍会标记为不可用，必须改成有效选择后才能保存。保存接口会再次校验模型是否属于当前分组和平台，并逐一校验渠道与模型是否匹配，避免绕过前端写入无效绑定；空渠道列表仍表示显式关闭有效绑定。

相关权限包括：

- 异步图像任务读取和管理。
- 图像内容审核读取和管理。
- 图像配置读取和管理。

前端为新增功能补齐 en、zh、zh-TW、fr、ja、ru、vi 七种语言。

## 迭代记录

| 提交 | 结果 |
| --- | --- |
| `c211e365` | 建立异步图像、工作台、图库、广场、审核、存储和 worker 的完整基础。 |
| `f10ffe5d` | 扩展任务管理 API 和完整任务详情，新增后端及前端测试。 |
| `6e9a9c31` | 加入徽章和视觉映射，优化参考图操作，默认关闭自动刷新。 |
| `f5c05a1d` | 减少查询次数，预取详情，按需获取图像 URL。 |
| `75e4f734` | 使用签名对象 URL 并完善任务结果的异步加载。 |
| 待提交（基于 `75aec941`） | 约束工作台高度链并保留内容滚动，防止模型附加参数和生成按钮被底部裁掉。 |
| 待提交（基于 `ead8bfcc`） | 将可执行 outbox 改为秒级投递，修复上游已接单任务的租约释放、延时轮询和“处理中”状态。 |
| 待提交（基于 `4f6a36ee`） | 为平台策略加入随平台切换的模型能力示例弹窗、一键复制、Gemini 专用说明和七种语言。 |
| 待提交（基于 `3ac4b1f2`） | 将渠道池模型和渠道 ID 改为平台联动选择，编辑时回填原配置，并在保存端校验候选资格。 |
| 待提交（基于 `579d578e`） | 增加渠道最大参考图数量设置；超过 8 张时优先调度容量覆盖请求的渠道，并将全局默认与模型策略示例调整为 14。 |
| 待提交（基于 `579d578e`） | 管理员任务详情参考图区域支持一键复制全部可访问 URL（每行一个）。 |
| 待提交（基于 `37e32db1`） | 修正 Gemini 参考图透传回退链路，移除合计字节/像素限制，安全规范化短尾随数据，并将全局参考图数量提升为普通表单项。 |
| 待提交（基于 `b3c6929d`） | 对首次远程 `fileData` 的模糊 Gemini 400 最多执行一次本地传输回退，并在严格 JSON 编辑器外提供完整运行字段说明。 |
| 待提交（基于 `28878b52`） | 增加渠道级上游异步规则，动态解析任务号、轮询状态、多个图片结果和实际用量，并在渠道编辑页提供结构化与 JSON 配置。 |
| `74c89aea2` | 保留 Gemini 异步上游脱敏错误诊断，修复 611–613 分类并记录 provider code 与 request ID。 |

逐提交文件规模和详细摘要见 [2026 年 9 月 Git 变更台账](02-2026年09月代码变更台账.md)。
