# 异步图片与视频调度配置说明

本文说明如何在 New API 中配置异步图片、异步视频，以及如何按模型、尺寸、优先级和权重调度不同渠道账号。

> 当前图片和视频共用“平台策略 + 渠道池”调度配置。视频相关配置目前也位于“系统设置 → 运营设置 → 图片设置”中。

## 一、先理解优先级和权重

### 1. 优先级决定先使用哪一层渠道

- 优先级数值越大，越先被使用。
- 优先级 `0` 是正常有效的默认值，不代表禁用。
- 只有当前优先级的渠道无法完成请求，并且请求进入重试流程时，才会尝试下一档较低优先级。

例如：

| 渠道 | 优先级 | 权重 | 实际作用 |
|---|---:|---:|---|
| A | 10 | 80 | 正常请求首先使用 A |
| B | 0 | 20 | A 失败并触发重试时，才可能使用 B |

在这个例子中，B 不会因为权重为 `20` 就获得 20% 的正常流量。A 和 B 的优先级不同，系统会先使用 A。

如果希望 A 多调度、B 少调度，应让它们处于相同优先级，再用权重分流：

| 渠道 | 优先级 | 权重 | 大致流量 |
|---|---:|---:|---:|
| A | 0 | 80 | 80% |
| B | 0 | 20 | 20% |

### 2. 权重只在相同优先级内生效

常用配置：

| 目标 | 配置方法 |
|---|---|
| A、B 平均分流 | 相同优先级，权重 `50 / 50` |
| A 多、B 少 | 相同优先级，权重 `80 / 20` 或 `90 / 10` |
| A 主用、B 备用 | A 使用较高优先级，B 使用较低优先级 |
| 完全停用某个渠道 | 禁用渠道，或将其从渠道池删除 |

### 3. 不建议使用权重 0 表示少量流量

不同调用路径对权重 `0` 的处理并不完全相同：

- 异步图片：同优先级存在正权重渠道时，权重为 `0` 的渠道基本不会获得正常流量。
- 异步视频：权重为 `0` 时会按最低有效权重 `1` 处理，仍可能获得少量流量。
- 普通接口：还会受到是否启用内存缓存等运行配置影响。

为了让行为明确，建议始终使用正数权重：

- 主渠道：`80`、`90` 等较大正数。
- 少量流量渠道：`20`、`10` 等较小正数。
- 备用渠道：降低优先级。
- 停用渠道：直接禁用或移出渠道池。

## 二、推荐的渠道组织方式

### 一个上游账号建立一个渠道

如果有两个上游账号，建议分别建立两个渠道。例如：

| 渠道 ID | 上游账号 | 分组 | 优先级 | 权重 |
|---:|---|---|---:|---:|
| 11 | 账号 A | `default` | 0 | 80 |
| 12 | 账号 B | `default` | 0 | 20 |

不要把需要独立调度的账号都放进同一个多密钥渠道。一个渠道中的多把密钥共用渠道的优先级和权重，无法分别配置流量比例。

渠道还需要满足以下条件，才能进入异步媒体调度：

- 渠道已启用。
- 渠道属于请求 Token 使用的分组。
- 渠道的模型列表包含用户请求的对外模型。
- 对应渠道适配器或任务插件支持该媒体能力。
- 如果上游真实模型名和对外模型名不同，已配置模型映射。

## 三、开启异步图片

进入：

**系统设置 → 运营设置 → 图片设置 → 运行设置**

打开“异步图片生成”。

异步图片还需要以下运行条件：

1. Redis 正常可用。
2. 在“图片存储”中配置并启用一个 `temporary` 存储配置。本地存储可以使用。
3. 配置稳定的任务加密密钥。多实例部署时，所有实例必须使用相同密钥。

环境变量示例：

```env
ASYNC_IMAGE_ACTIVE_KEY_ID=images-1
ASYNC_IMAGE_PAYLOAD_KEYS=images-1:<32字节随机密钥的Base64值>
ASYNC_IMAGE_REDIS_PREFIX=new-api:images
```

## 四、图片按模型和尺寸调度

下面以两个 OpenAI 图片账号为例：

- 渠道 11：账号 A。
- 渠道 12：账号 B。
- 对外模型：`gpt-image-1`。
- 分组：`default`。

### 1. 配置渠道

| 渠道 | 模型 | 分组 | 通用优先级 | 通用权重 |
|---|---|---|---:|---:|
| 11 | `gpt-image-1` | `default` | 0 | 80 |
| 12 | `gpt-image-1` | `default` | 0 | 20 |

### 2. 建立图片平台策略

进入“图片设置 → 平台策略”，新增策略：

- 分组：`default`
- 平台：选择渠道对应的供应商；OpenAI 及相应兼容渠道通常选择 `openai`
- 渠道池绑定：`model_resolution`
- 启用：打开
- 异步：打开

三种渠道池绑定方式的含义：

| 绑定方式 | 作用 |
|---|---|
| `resolution` | 只按 `1K / 2K / 4K` 区分渠道池，所有模型共用 |
| `model` | 只按模型区分渠道池，不区分尺寸 |
| `model_resolution` | 按“模型 + 尺寸”分别配置渠道池 |

需要按不同模型和尺寸指定账号时，应选择 `model_resolution`。

模型能力目录示例：

```json
[
  {
    "id": "gpt-image-1",
    "label": "GPT Image 1",
    "resolutions": ["1K", "2K", "4K"],
    "qualities": ["low", "medium", "high"],
    "formats": ["png", "webp"],
    "backgrounds": ["auto", "transparent", "opaque"],
    "max_output_images": 4,
    "max_reference_images": 4
  }
]
```

其中 `id` 必须与渠道提供的对外模型名一致。

常见图片平台标识包括：

| 类型 | 平台标识 |
|---|---|
| OpenAI 及相应兼容图片协议 | `openai` |
| Gemini | `gemini` |
| 阿里图片 | `ali` |
| 豆包/火山图片 | `volcengine` |
| MiniMax | `minimax` |
| 即梦 | `jimeng` |
| xAI | `xai` |
| Replicate | `replicate` |
| 智谱 | `zhipu_v4` |
| Vertex AI 图片 | `vertex` |

实际配置时，以页面显示的平台选项和渠道适配器为准。

### 3. 建立图片渠道池

#### 示例 A：1K 图片按 80% / 20% 分流

建立 `gpt-image-1 + 1K` 渠道池：

| 渠道 | 池内优先级 | 渠道全局权重 |
|---|---:|---:|
| 11 | 0 | 80 |
| 12 | 0 | 20 |

池内优先级相同，因此系统会使用渠道的全局权重分流。

#### 示例 B：2K 图片让账号 B 主用、A 备用

建立 `gpt-image-1 + 2K` 渠道池：

| 渠道 | 池内优先级 |
|---|---:|
| 12 | 10 |
| 11 | 0 |

正常请求先进入渠道 12。渠道 12 失败并触发重试时，才会尝试渠道 11。

#### 示例 C：4K 图片只允许账号 B

建立 `gpt-image-1 + 4K` 渠道池，只加入渠道 12。

如果为某个“模型 + 尺寸”保存了空渠道列表，该组合会被明确关闭。没有建立对应绑定时，系统会从所有符合条件的渠道中按照通用优先级和权重选择。

### 4. 请求中明确传递尺寸档位

为了稳定匹配渠道池，建议请求同时提供 `resolution` 和实际 `size`：

```json
{
  "model": "gpt-image-1",
  "prompt": "一只猫",
  "resolution": "2K",
  "size": "2048x2048"
}
```

`resolution` 用于匹配 `1K / 2K / 4K` 渠道池，`size` 用于描述实际输出尺寸。

## 五、开启异步视频和本地存储

进入：

**系统设置 → 运营设置 → 图片设置 → 运行设置**

打开“异步视频和本地存储”，然后配置：

| 设置 | 默认值或建议 |
|---|---|
| 本地存储目录 | 服务器上可写且空间充足的目录 |
| 视频保存期限 | 默认 90 天 |
| 结果链接有效期 | 默认 1 小时 |
| 单文件上限 | 默认 1 GiB |
| 下载超时 | 默认 15 分钟 |
| 下载并发 | 默认 2 |

视频生成完成后，系统会将上游视频下载到本地。只有文件保存成功后，任务才会返回本站结果链接。

## 六、视频按模型和尺寸调度

下面以两个 Sora 视频账号为例：

| 渠道 | 视频模型 | 分组 | 通用优先级 | 通用权重 |
|---|---|---|---:|---:|
| 21 | `sora-2` | `default` | 0 | 80 |
| 22 | `sora-2` | `default` | 0 | 20 |

### 1. 建立视频平台策略

在“图片设置 → 平台策略”中新增：

- 分组：`default`
- 平台：`sora`
- 渠道池绑定：`model_resolution`
- 启用：打开
- 异步：打开

当前常见视频平台标识包括：

- `sora`
- `alibaba`
- `doubao`
- `jimeng`
- `kling`
- `hailuo`
- `vidu`
- `google`
- `vertex-ai`

注意：阿里图片平台使用 `ali`，阿里视频任务平台使用 `alibaba`。配置时以页面对应渠道显示的选项为准。

视频模型目录可以先填写：

```json
[
  {
    "id": "sora-2",
    "label": "Sora 2",
    "resolutions": ["1K", "2K", "4K"],
    "max_output_images": 1,
    "max_reference_images": 1
  }
]
```

当前图片和视频共用能力目录编辑结构，因此这里仍会出现 `max_output_images` 等图片字段。视频调度真正需要匹配的是模型 ID 和尺寸档位。

### 2. 视频尺寸的归类规则

视频按照宽高中的较短边归类：

| 请求尺寸 | 渠道池档位 |
|---|---|
| 较短边不超过 1024，例如 `1280x720` | `1K` |
| 较短边为 1025～2048，例如 `1920x1080` | `2K` |
| 较短边超过 2048，例如 `3840x2160` | `4K` |

### 3. 建立视频渠道池

可以按照下面的方式配置：

- `sora-2 + 1K`：渠道 21 和 22 的池内优先级都设为 `0`，使用全局权重 `80 / 20` 分流。
- `sora-2 + 2K`：渠道 22 的池内优先级设为 `10`，渠道 21 设为 `0`，让 22 主用、21 备用。
- `sora-2 + 4K`：只加入真正支持 4K 的渠道。

当前视频可以按模型和尺寸调度，还不能按视频时长建立渠道池。

## 七、请求中的 provider 参数

异步媒体请求可以选择性提供网关专用的 `provider`：

```json
{
  "model": "gpt-image-1",
  "provider": "openai",
  "prompt": "一座雪山"
}
```

- 指定 `provider` 后，只会在该供应商范围内选择渠道。
- 不指定时，系统根据分组、模型能力、平台策略、渠道池、优先级和权重自动选择。
- `provider` 只供网关调度使用，不会转发给上游。

一般用户请求可以不传 `provider`，由管理员在平台策略和渠道池中统一控制。

## 八、当前限制

### 1. 渠道池没有独立权重

渠道池可以针对“模型 + 尺寸”设置渠道和池内优先级，但权重仍然使用渠道的全局权重。

因此，同一组渠道目前不能直接表达：

- 1K：A 90%，B 10%。
- 2K：A 20%，B 80%。

临时做法是为不同尺寸或调度角色建立独立渠道记录，并分别设置权重。它们可以使用相同上游账号，但会作为不同调度项参与选择。

### 2. 视频设置名称仍偏向图片

视频复用了现有图片媒体设置，因此部分页面名称和能力字段仍带有“图片”字样。这不会阻止视频任务调度和本地存储。

### 3. Token 平台分组界面尚未完整展示所有供应商

后端可以处理已注册的媒体供应商，但当前 Token 编辑界面主要显示 OpenAI 和 Gemini 平台映射。最简单稳定的配置方式，是将相关图片和视频策略配置在 Token 的主分组中。

## 九、快速配置模板

如果目标是“主账号获得 80% 流量，次账号获得 20% 流量”：

1. 每个账号建立一个独立渠道。
2. 两个渠道使用相同分组和模型。
3. 两个渠道优先级都设置为 `0`。
4. 主渠道权重设置为 `80`，次渠道权重设置为 `20`。
5. 建立 `model_resolution` 平台策略。
6. 在对应“模型 + 尺寸”渠道池中加入两个渠道，并让池内优先级相同。

如果目标是“主账号失败后才使用备用账号”：

1. 主渠道优先级设置为 `10`。
2. 备用渠道优先级设置为 `0`。
3. 在对应渠道池中保持相同的优先级关系。
4. 权重不会把备用渠道变成日常分流渠道；只有进入较低优先级重试时才会使用它。

